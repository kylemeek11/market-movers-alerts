name: Stop order check

on:
  schedule:
    # Times are UTC. During CDT (UTC-5) these land at roughly 3am, 7am, 11am,
    # 3pm, 7pm and 11pm Central - every four hours, all equally sensitive
    # (quiet hours are switched off in stop_check.py). After the November 1
    # switch to CST they all shift an hour earlier; adjust then if it matters.
    - cron: "0 0,4,8,12,16,20 * * *"
  workflow_dispatch:
    inputs:
      force_alert:
        description: "Send the full rundown even if nothing needs changing"
        type: boolean
        default: false

concurrency:
  group: stop-check
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 6

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Restore stop state
        uses: actions/cache/restore@v4
        with:
          path: state
          key: stop-state-${{ github.run_id }}
          restore-keys: stop-state-

      - name: Recalculate stops
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          FORCE_ALERT: ${{ inputs.force_alert && '1' || '0' }}
        run: |
          mkdir -p state
          python stop_check.py

      - name: Save stop state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: state
          key: stop-state-${{ github.run_id }}
